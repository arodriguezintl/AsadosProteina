const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); const env = fs.readFileSync('.env', 'utf8'); const url = env.match(/VITE_SUPABASE_URL=([^\n]+)/)[1].trim(); const key = env.match(/VITE_SUPABASE_ANON_KEY=([^\n]+)/)[1].trim(); const supabase = createClient(url, key); supabase.from('stores').select('*').then(({data}) => { console.log(JSON.stringify(data, null, 2)); process.exit(0); });
